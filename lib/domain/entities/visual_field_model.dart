
import 'visual_row_model.dart';

/// Clase visual que representa las cabeceras de la base del conocimiento.
///
class VisualFieldModel {
  final String field;
  final List<VisualRowModel> row;

  VisualFieldModel(this.field, this.row);
}